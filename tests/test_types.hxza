// Test type annotations
func add(x: int, y: int) -> int {
    return x + y
}

let result: int = add(5, 10)
print("Result:", result)

// Test with strings
func greet(name: string) -> string {
    return "Hello, " + name
}

const message: string = greet("Hexza")
print(message)
